<% unless @inactive_stores.empty? %>
  <div class="panel callout radius">
    <h3>Inactive Stores</h3>
    <ul>
      <% @inactive_stores.each do |store| %>
        <li><%= link_to store.name, store_path(store) %></li>
      <% end %>
    </ul>
  </div>
<% end %>